import styled from 'styled-components';

const Box = styled.div`
  background: #fff;
  margin-bottom: 15px;
  box-shadow: 0 0 1px 0 rgba(0, 0, 0, 0.5);
  border-radius: 5px;
  font-size: 16px;
  line-height: 1;
`;

export default Box;
