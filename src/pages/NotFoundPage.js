import React from 'react';
import styled from 'styled-components';

const AmountText = styled.div`
  font-size: 2rem;
  font-weight: 500;
  margin: 30px 0 20px 0;
`;

const NotFoundPage = () => <AmountText>Страница не найдена</AmountText>;

export default NotFoundPage;
